default:
  suites:
    list:
      paths:
        - "%paths.base%/features/list.feature"
      contexts:
        - "SharedContext"
        - "ListContext"
    create:
      paths:
        - "%paths.base%/features/create.feature"
      contexts:
        - "SharedContext"
        - "CreateContext"
